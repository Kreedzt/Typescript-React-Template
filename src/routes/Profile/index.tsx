import React from 'react';
import { Tabs } from 'antd';

const { TabPane } = Tabs;

const MyProfile: React.FC = () => {
  return (
    <div>
      Profile page...
    </div>
  );
};

export default MyProfile;
